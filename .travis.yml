language: node_js

node_js:
  - "8"

dist: trusty
sudo: required


branches:
  only:
  - master
  - dev

install:
  - npm install

script:
  - npm build
  - sonar-scanner

cache:
  directories:
    - node_modules